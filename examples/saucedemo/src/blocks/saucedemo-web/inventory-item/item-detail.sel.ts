/**
 * DOM selectors for the item detail page (`/inventory-item.html`) and the
 * order-complete page's equivalent nav control. Convention: DOM strings
 * live in `*Sel` next to the page, not inlined in each Block.
 */
export const ItemDetailSel = {
  continueShoppingBtn: "#continue-shopping",
  backToProductsBtn: "#back-to-products",
};
