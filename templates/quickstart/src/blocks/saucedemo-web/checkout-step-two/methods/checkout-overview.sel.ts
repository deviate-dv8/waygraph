/**
 * DOM selectors for the order overview / completion page
 * (`/checkout-step-two.html`). Convention: DOM strings live in `*Sel` next
 * to the page, not inlined in each Block.
 */
export const CheckoutOverviewSel = {
  finishBtn: "#finish",
  completeHeader: ".complete-header",
};
