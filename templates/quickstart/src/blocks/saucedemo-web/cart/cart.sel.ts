/**
 * DOM selectors for the cart route (`/cart.html`) and its outbound nav
 * controls. Convention: DOM strings live in `*Sel` next to the page, not
 * inlined in each Block.
 */
export const CartSel = {
  cartLink: ".shopping_cart_link",
  checkoutBtn: "#checkout",
};
